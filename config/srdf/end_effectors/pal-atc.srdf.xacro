<?xml version="1.0" ?>
<robot xmlns:xacro="http://ros.org/wiki/xacro">
    <xacro:property name="end_effector_name" value="gripper" />
    <xacro:macro name="define_end_effector" params="arm name">
        <group name="${name}">
            <link name="${name}_pal_atc_base_link" />
            <link name="${name}_tool_mount" />
            <link name="${name}_tool_holder" />
            <joint name="${name}_tool_mount_joint" />
        </group>
        <!--PASSIVE JOINT: Purpose: this element is used to mark joints that are not actuated-->
        <passive_joint name="${name}_pal_atc_base_joint" />

        <!--END EFFECTOR: Purpose: Represent information about an end effector.-->
        <end_effector name="${name}" parent_link="${arm}_tool_link" group="${name}" parent_group="${arm}" />
    </xacro:macro>

</robot>
